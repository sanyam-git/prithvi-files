#include<stdio.h>
#include<string.h>
#include<stdlib.h>
//names should be single word
int main()

void sort(int n,char *arr[n])
{
in;
}

{int n;
scanf("%d",&n);
char temp[100];
int len,i;
char *arr[n];
for(i=0;i<n;i++)
{	scanf("%s",temp);
	len=strlen(temp);

arr[i]=malloc(len);
strcpy(arr[i],temp);
}
sort(arr,n);

for(i=0;i<n;i++)
printf("%s\n",arr[i]);

return 0;

}
